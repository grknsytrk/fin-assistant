paths:
  processed_dir: data/processed

kap:
  enabled: true
  timeout_seconds: 10.0
  # 0 = her zaman canli KAP; basarili yanit cache'e yazilir.
  cache_ttl_hours: 0.0
  user_agent: ragfin-kap-fetcher/1.0 (+local-first)
